export enum EWarnRecSystem {
  CONFIG_NOT_FOUND = 'Configuration is disabled or not found',
  NOT_STRATEGY_OR_SCOPE = 'Strategy was not found in the config or has an invalid scope',
  TRIGER_ALREADY_RUNNING = 'Trigger generation already running',
}
